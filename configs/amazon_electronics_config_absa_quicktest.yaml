# configs/amazon_electronics_config_absa_quicktest.yaml
# Smoke test dari amazon_electronics_config_absa.yaml pada subset kecil.
# PRASYARAT: jalankan run_baseline.py --config amazon_electronics_config_quicktest.yaml
# SEKALI dulu sampai tahap 4 selesai.

_base: amazon_electronics_config_quicktest.yaml

experiment:
  name: "amazon_electronics_absa_ablation_quicktest"

absa:
  aggregation: "mean"
