# configs/amazon_electronics_config_absa_confidence_colab.yaml
# Versi Colab dari amazon_electronics_config_absa_confidence.yaml.

_base: amazon_electronics_config_colab.yaml

experiment:
  name: "amazon_electronics_absa_confidence_ablation_colab"

absa:
  mode: "confidence_mean"
  aggregation: "mean"
