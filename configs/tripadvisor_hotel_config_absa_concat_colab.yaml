# configs/tripadvisor_hotel_config_absa_concat_colab.yaml
# Versi Colab dari tripadvisor_hotel_config_absa_concat.yaml.

_base: tripadvisor_hotel_config_colab.yaml

experiment:
  name: "tripadvisor_hotel_absa_concat_ablation_colab"

absa:
  mode: "concat"
  aggregation: "mean"
